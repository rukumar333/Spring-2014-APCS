Although the code for my knights tour is much more complicated than what we did in class,
it still works. The three methods solve(), toString(), Knight(int x) are all present along with
some helper methods such as inBounds() which checks if the coordinates are in bounds. 
