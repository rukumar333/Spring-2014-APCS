public class MergeSort{

    int[] merge (int[]a;int[]b){
	while(a.length > 0 || b.length > 0){
	    if(a.length == 0){
		int blen = b.length;
		for(int k = 0; k < blen; k ++){
		    merge.add(b.remove(0));
		}
	    }
	    if(b.length == 0){
		int alen = b.length;
		for(int k = 0; k < alen; k ++){
		    merge.add(a.remove(0));
		}
	    }
	    if(
	}
    }
}